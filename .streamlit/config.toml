# Tema por defecto (F3.4). El usuario puede cambiarlo en ⋮ → Settings → Theme.
[theme]
base = "dark"
primaryColor = "#3b82f6"
backgroundColor = "#0f1117"
secondaryBackgroundColor = "#161b22"
textColor = "#e6edf3"
font = "sans serif"
